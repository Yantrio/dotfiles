onboarding = false

[theme]
name = "kanagawa"
auto_switch = false

[theme.custom]
surface_dim = "#727169"   # keep — borders
text        = "#FF0000"   # RED — diagnostic: if the highlight bg goes red too, we found it
