onboarding = false

[theme]
name = "kanagawa"
auto_switch = false

[theme.custom]
surface_dim = "#727169"   # Fuji Grey — pane divider
mauve       = "#957FB8"   # Oni Violet — try as selected-workspace/agent highlight
# accent left default (Crystal Blue in built-in kanagawa) — was making the highlight
# too bright previously; if mauve doesn't drive the highlight we'll iterate.
